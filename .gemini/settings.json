{
  "hooks": {
    "BeforeTool": [
      {
        "matcher": "read_file|list_directory",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const fs = require('fs'); const exists = fs.existsSync('graphify-out/graph.json'); console.log(JSON.stringify({ decision: 'allow', additionalContext: exists ? 'graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files.' : undefined }));\""
          }
        ]
      }
    ]
  },
  "mcp": {
    "servers": {
      "ruflo": {
        "command": "npx",
        "args": ["-y", "ruflo", "mcp", "start"]
      },
      "github": {
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-github"],
        "env": {
          "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
        }
      }
    }
  }
}